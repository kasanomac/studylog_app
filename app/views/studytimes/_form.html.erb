<div class="form-container">
  <%= form_with(model: studytime) do |form| %>
    <% if studytime.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(studytime.errors.count, "error") %> prohibited this studytime from being saved:</h2>

        <ul>
          <% studytime.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

      <div class="form-group">
        <p>勉強時間</p>
        <%= form.select :studytime_hours,(0..23), id:"selectbox", class:"form-controll" %><strong>時間</strong>
        <!--<%= form.label :studytime_hours, :"時間" %>-->
        <%= form.select :studytime_minutes,(00..59), id:"selectbox", class:"form-controll" %><strong>分</strong>
        <!--<%= form.label :studytime_minutes, :"分"%>-->
      </div>

    <div class="form-group">
      <%= form.label :memo, :"メモ", style: "display: block", for: "create_memo" %>
      <%= form.text_area :memo, size:"30*5", id: 'create_memo', class: 'form-controll' %>
    </div>

    <div class="form-group">
      <%= form.label :image, :"画像", style: "display: block", for: "create_image" %>
      <%= form.file_field :image, id: 'create_image', class: 'form-controll' %>
    </div>

    <div class="action">
      <%= form.submit class:"btn btn-outline-info" %>
    </div>
  <% end %>
</div>